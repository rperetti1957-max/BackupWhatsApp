name: Build Android APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Imposta Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Installa Buildozer e Dipendenze
        run: |
          sudo apt update
          sudo apt install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libssl-dev cmake
          pip3 install --user --upgrade buildozer cython virtualenv

      - name: Compila APK con Buildozer
        run: |
          export PATH=$PATH:~/.local/bin
          buildozer android debug

      - name: Carica l'APK finale
        uses: actions/upload-artifact@v4
        with:
          name: WhatsAppMover-APK
          path: bin/*.apk
